# Project-level npm configuration.
# Default to the public npm registry; the k8s CI build appends the internal
# Verdaccio registry (VERDACCIO_REGISTRY) at build time, which takes precedence.
registry=https://registry.npmjs.org/
